The instance of `LoadingCache` is created by using a factory method on the `CacheBuilder` Class. 

```java
CacheBuilder.newBuilder().build(...)
```

[What do we pass into the build method?](./hints/hint-02b.md)
